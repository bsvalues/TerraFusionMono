{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Pack executor",
  "description": "Creates a package for distribution",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The output directory for the package.",
      "default": "dist/pack"
    },
    "includeFiles": {
      "type": "array",
      "description": "List of file patterns to include in the package.",
      "items": {
        "type": "string"
      },
      "default": ["terra.json", "README.md", "LICENSE"]
    },
    "generateChecksums": {
      "type": "boolean",
      "description": "Generate checksums for all files in the package. Creates a checksums.json file.",
      "default": true
    }
  },
  "required": ["outputPath", "includeFiles"]
}