{
  "name": "pacs-migration-pack",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "bundles/pacs-migration-pack/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "mkdir -p dist/bundles/pacs-migration-pack"
          },
          {
            "command": "cp -r bundles/pacs-migration-pack/scripts dist/bundles/pacs-migration-pack/"
          },
          {
            "command": "chmod +x dist/bundles/pacs-migration-pack/scripts/pacs_wizard.sh"
          }
        ],
        "parallel": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["bundles/pacs-migration-pack/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/bundles/pacs-migration-pack"],
      "options": {
        "jestConfig": "bundles/pacs-migration-pack/jest.config.ts"
      }
    },
    "pack": {
      "executor": "@terrafusion/nx-pack:pack",
      "outputs": ["{options.outputPath}/pacs-migration-pack"],
      "options": {
        "outputPath": "dist/pack",
        "includeFiles": [
          "terra.json",
          "README.md",
          "LICENSE",
          "scripts/**/*",
          "docs/**/*",
          "config/**/*"
        ],
        "excludeFiles": [
          "node_modules/**/*",
          "**/*.test.ts",
          "**/*.spec.ts"
        ],
        "generateChecksums": true,
        "validateSchema": true,
        "generateSBOM": true,
        "sbomFormat": "cyclonedx"
      }
    }
  },
  "tags": []
}